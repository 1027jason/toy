name: application
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:

  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^1.10.0  # 최신 버전을 확인하고 입력하세요
  cloud_firestore: ^3.1.6  # 최신 버전을 확인하고 입력하세요
  google_sign_in: ^5.2.1
  firebase_auth: ^3.1.6  # 최신 버전을 확인하고 입력하세요

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
